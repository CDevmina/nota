{
  "collectionName": "components_sections_heros",
  "info": {
    "displayName": "Hero",
    "icon": "picture",
    "description": "Full-bleed pen render with a two-line display headline."
  },
  "options": {},
  "attributes": {
    "anchorId": {
      "type": "string",
      "regex": "^[a-z0-9-]*$",
      "description": "Rendered as the section's HTML id. Nav links point at #<anchorId>."
    },
    "headlineTop": {
      "type": "string",
      "required": true
    },
    "headlineBottom": {
      "type": "string",
      "required": true
    },
    "media": {
      "type": "component",
      "repeatable": false,
      "component": "shared.media"
    },
    "frames": {
      "type": "media",
      "multiple": true,
      "allowedTypes": [
        "images"
      ],
      "description": "Scroll-scrubbed frame sequence, in order. The reference plays 75 frames across the hero; leave empty to fall back to the still image."
    }
  }
}
